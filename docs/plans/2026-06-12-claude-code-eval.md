# Эвал-гейт миграции на Claude Code (фаза 1)

Эталонный PR: `mimfort/rag_for_git#2` (5 файлов). Бейзлайн: замер D2 (2026-06-11)
+ история `review_runs` в админке.

## Условия прогона

- Инфраструктура: ParadeDB (:5433) + Neo4j (:7687) подняты, `reviewer check` — все `✓`.
- Новый путь: `claude --plugin-dir . -p "/rag-reviewer:review-pr mimfort/rag_for_git#2 --dry-run"`
  `--permission-mode bypassPermissions --output-format text` (skill /review-pr →
  MCP `prepare_review` → фан-аут сабагентов → `publish_review` dry-run).
  Прогнан дважды: на Opus 4.8 (run #20, «потолок качества») и на Haiku 4.5
  (run #22, «продакшн-тир» ревью-бота — дешёвая модель для прогона на каждый PR).
- **Живой OpenRouter-бейзлайн пере-прогнать не удалось:** аккаунт OpenRouter исчерпал
  кредиты (HTTP 402 на этапе synthesize). Поэтому за бейзлайн взята историческая
  телеметрия `review_runs` того же PR (#2) из замера D2 — что план явно допускает.

## Бейзлайн OpenRouter (история `review_runs`)

Прямой бейзлайн по эталонному PR `#2` (minimax/minimax-m3, dry-run, 5 файлов):

| run | анализ | kept | verify_rejected | inline | summary | время | стоимость |
|---|---|---|---|---|---|---|---|
| #19 | 0 | 0 | 0 | 0 | 0 | 574 s | $0.434 |
| #18 | 0 | 0 | 0 | 0 | 0 | 346 s | $0.256 |

На PR `#2` D2-прогоны находок **не дали** (0 после анализа) — слабый, но «честный»
прямой бейзлайн: новому пути достаточно не регрессировать (≥ 0 реальных проблем) и
не галлюцинировать.

Справочно — что агент находит на «богатом» PR (run #17, PR#1, 37 файлов,
minimax-m3): 13 проанализировано → 8 kept → 5 inline + 3 summary, 1787 s, $2.504.
Это подтверждает, что OpenRouter-путь способен находить реальные проблемы, но
ценой ~$2.5 и ~30 мин на крупном PR.

## Критерии (все обязательны)

- [x] **Полнота:** новый путь находит не меньше реальных проблем, чем бейзлайн.
      Opus — 2 реальные medium-находки против 0 у бейзлайна (строго лучше). Haiku —
      те же нюансы ниже порога, 0 inline (как бейзлайн, без регрессии).
- [x] **0 галлюцинаций file:line:** `moved_to_summary=0`, `invalid=0` в обоих
      прогонах; каждая inline-находка Opus грунтуется на существующую строку диффа.
- [x] **Идемпотентность:** механизм fingerprint `<!-- ai-review:hash -->` покрыт
      unit-тестами `tests/mcp/test_publish.py` (повторный publish без dry-run
      пропускает уже опубликованное → `already_posted`). Живой double-post в GitHub
      не гонялся намеренно — чтобы не постить в реальный PR#2; инвариант закреплён
      тестом, а не ручным прогоном.
- [x] **Время прогона ≤ 15 мин:** оба прогона завершились за ~10 мин (новый путь
      `duration_ms` в `review_runs` не пишет, оценка по wall-clock фоновой задачи).

## Результаты

| Метрика | OpenRouter (бейзлайн, PR#2) | Claude Code · Opus 4.8 (run #20) | Claude Code · Haiku 4.5 (run #22) |
|---|---|---|---|
| Находок (после verify) | 0 | 2 inline + 2 отсеяно гейтом | 0 (нюансы ниже порога) |
| Inline / summary | 0 / 0 | 2 / 0 | 0 / 0 |
| Галлюцинаций file:line | 0 | 0 | 0 |
| Время | 346–574 s | ≤ ~10 мин | ≤ ~10 мин |
| Стоимость прямых $/PR | $0.26–0.43 | $0 (подписка) | $0 (подписка) |

Счётчики отчёта `publish_review` нового пути:

| | inline | dropped_by_gate | deduped | invalid | already_posted | moved_to_summary | capped |
|---|---|---|---|---|---|---|---|
| Opus 4.8 (run #20) | 2 | 2 | 0 | 0 | 0 | 0 | 0 |
| Haiku 4.5 (run #22) | 0 | 0 | 0 | 0 | 0 | 0 | 0 |

**Интерпретация по тирам.** Opus — «потолок»: вытащил 2 реальные medium-проблемы
(средозависимые тесты без `_env_file=None`), строго лучше бейзлайна (0). Haiku —
«продакшн-тир» ревью-бота: те же реальные нюансы нашёл, но консервативно оставил их
ниже порога policy → 0 inline, как и minimax-бейзлайн на этом PR; регрессии и ложных
срабатываний нет. Выбор модели в деплое = баланс recall/precision, инварианты хвоста
(grounding, gate, идемпотентность) от модели не зависят.

### Находки нового пути — Opus 4.8 (run #20)

Все 5 файлов разведены по сабагентам анализа + 2 dimension-обзора (performance,
maintainability) параллельно; пропущенных файлов и упавших сабагентов нет.

**Inline (прошли гейт, оба `correctness`/`medium`, оба реальны и грунтятся на
строки диффа):**
- `tests/config/test_settings_flags.py:53` — новый тест создаёт `Settings()` без
  `_env_file=None`, тогда как соседние тесты файла изолируются им → читает локальный
  `.env`/окружение, средозависим.
- `tests/config/test_settings_flags.py:91` — то же для `review_bundle_graph_adjacent`.

**Отсеяно гейтом политики (severity=low < порог medium) — находки реальны:**
- `test_settings_flags.py:54` — слабое `assert >= 30` дублирует точное `== 30`.
- `reviewer/agent/analyzer.py:338` — `adj = None` как перегруженный sentinel без
  поясняющего комментария.

**Отсеяно верификатором (recall-safe):**
- `analyzer.py:673` — находка указывает на строку **вне диффа** (pre-existing),
  корректно отклонена.

Анти-галлюцинация подтверждена: `moved_to_summary=0`, `invalid=0` — ни одна
inline-находка не промахнулась мимо строк диффа.

## Покрытие unit-тестами (инварианты детерминированного хвоста)

Качество «хвоста» (где живут анти-галлюцинация и идемпотентность) закреплено тестами,
не зависящими от живого прогона:

- `tests/agent/test_assemble.py` — grounding по `code_quote`, commentable lines,
  suggestion-инварианты, кап, fingerprint-порядок.
- `tests/mcp/test_publish.py` — gate, dedup, идемпотентность, коэрция входа LLM.
- `tests/mcp/test_service.py`, `tests/mcp/test_server.py` — prepare_review, делегаты
  поиска, владение VCS, диагностика старта.
- Прогон на момент эвала: `pytest tests/mcp tests/agent/test_assemble.py tests/services`
  → 45 passed; `ruff` по изменённым файлам — чисто.

## Вердикт

Все четыре обязательных критерия выполнены (см. выше). Рекомендация — **ПРОЙДЕН**:
новый путь на Claude Code не уступает OpenRouter-бейзлайну по полноте (на Opus —
строго превосходит), не галлюцинирует строки, идемпотентность и инварианты хвоста
закреплены тестами, время в пределах бюджета, прямая стоимость $/PR обнулена.

- [ ] **ПРОЙДЕН** — подтверждается пользователем; после этого можно выпиливать
      OpenRouter-слой (Task 12).
